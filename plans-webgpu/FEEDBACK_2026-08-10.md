# Alfred feedback, 2026-08-10 — triage [ALL BUILT + REVIEWED 2026-08-10]

Status: text pass + both builder batches implemented; adversarial review
SHIP-WITH-NOTES, 0 major, 4 minor doc/text findings all fixed and re-gated.
Uncommitted — Alfred pushes. On-device list at the bottom stands.

Sixteen items, all on the webgpu apps. Design decisions taken with Alfred up front:
colorbar = small horizontal strip, right-aligned at the bottom of the display pane, in
line with the caption text; save/record = PNG (canvas.toBlob) + WebM (MediaRecorder,
native — no gif encoder shipped); tearing growth rate = γ_fit on the existing
island-width chart, legend shows γ = 2 × d(ln W)/dt (W ∝ e^{γt/2} in the linear
stage), with the KH mode-chart quality gates (fit window MODE_FIT_DT, rise, R²).

## A. Text pass (applied directly, this session)

1. Equation definitions in the caption under the displays: velocity u = ẑ×∇φ,
   magnetic b = ẑ×∇ψ, vorticity ω = ∇²φ, current j = ∇²ψ,
   σ_c = 2u·b/(|u|²+|b|²), σ_r = (|u|²−|b|²)/(|u|²+|b|²). (z± = u±b added for the
   Elsasser entries — same spirit.)
2. Energy-trace hint: drop "decimated 2:1 when full".
3. Energy-trace notation: E_kin, E_mag → E_u, E_b (legend labels, option text, hint).
4. Two-plot option labels use "," not "/" (energy trace and spectra).
5. Spectrum hint: drop "~3×/s", drop "y range follows the ⊥ spectra", drop
   "ghosts keep their physical k if the box changes".
6. Cut-trace hint → "cut along y at x = Lx/2".
7. Forced-2D hint → exactly: "Random power ε added into the ζ+ and ζ− fields via an
   Ornstein–Uhlenbeck process, with correlation time τ, in a chosen wave number band."
8. Decay hint: remove the amplitude self-similarity sentence.
10. Tearing hint: "reconnect into an island chain" → "reconnect into a magnetic island".

## B. Builder 1 (opus + adversarial Fable review)

9.  Tearing local growth rate: γ_fit on the island-width chart (decision above).
14. Paint selector reordered φ, ψ, ζ+, ζ− (currently ζ+, ζ−, φ, ψ; common.js ~2113).
15. Amplitude control for painting φ and ψ (ζ± amplitude sliders exist; φ/ψ painting
    currently has no amplitude knob).
16. New preset "rmhd variables": opens in custom blob mode set to paint φ, one big
    blob. Text: click once, Run → stable vortex, flow circling. φ contours on
    (contours of φ = streamlines of u). Explain vorticity. Then invite the same for ψ.
    Then: big φ blob at large amplitude + ψ lines at small amplitude — see what happens.

## C. Builder 2 (opus + adversarial Fable review)

11. README split: technical README stays repo-facing; new user-facing documentation
    page describing every button/feature, linked at the TOP of the app pages (the
    technical one is not).
12. Colorbar per display card (decision above): follows colormap + signed/unsigned
    range, min/0/max labels, drawn small enough not to fight the caption line.
13. Save button → current display canvas as PNG; record button → short WebM via
    captureStream + MediaRecorder, saved to the user's computer.

## On-device (Alfred, after build)

Colorbar size/legibility at phone widths; WebM record on iOS Safari (MediaRecorder
support caveat — degrade to save-only if absent); rmhd-variables preset feel;
γ_fit numbers vs the linear reference 0.0287 with maintain-flux on.
